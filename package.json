{
  "homepage": "https://sgmariakap.github.io/sg-nextjs-app/",
  "name": "my-nextjs-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "next build && next export -o out && touch out/.nojekyll && git add out && git commit -m 'Deploy to GitHub Pages' && git push origin gh-pages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "next": "^13.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
